# Monorepo Registry

The monorepo registry records known modules, domains, engines, packs, policies, and templates.

Canonical sources are distributed across `monad.toml`, `domains/`, `governance/`, and future generated `.monad/index` files.
