# Changesets

Changesets are included as a versioning engine surface for future package releases.

Monad's v1 release process remains governed by work packets, release plans, changelog evidence, and Git tags.
